services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - resume_uploads:/app/uploads
    environment:
      - PYTHONUNBUFFERED=1
    restart: always

volumes:
  resume_uploads:
